# LEGACY / EXPERIMENTAL — outside the supported v1 surface.
# v1 targets Claude Code only (see the repository README). This OpenAI/Codex
# adapter is retained for reference and future work; it is not part of the
# No Surprises v1 product and is not maintained against current Codex behaviour.
interface:
  display_name: No Surprises
  short_description: Keep consequential agent decisions visible
  default_prompt: Use $no-surprises while implementing this task so consequential
    decisions are surfaced before commitment.
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
